CREATE TABLE [dbo].[CATEGORIES] (
    [ID]        INT           IDENTITY (1, 1) NOT NULL,
    [PARENT_ID] INT           NULL,
    [NAME]      VARCHAR (150) NOT NULL,
    PRIMARY KEY CLUSTERED ([ID] ASC),
    CONSTRAINT [FK_CATEGORIES_PARENT_ID] FOREIGN KEY ([PARENT_ID]) REFERENCES [dbo].[CATEGORIES] ([ID])
);

